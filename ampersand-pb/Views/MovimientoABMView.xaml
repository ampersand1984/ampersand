<UserControl x:Class="ampersand_pb.Views.MovimientoABMView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="Green">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="10"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="10"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
            </Style>
        </Grid.Resources>

        <TextBlock Grid.Row="0" Text="Id movimiento"/>
        <TextBox Grid.Row="0" Text="{Binding IdMovimiento}" Grid.Column="1" IsEnabled="False"/>

        <TextBlock Grid.Row="1" Text="Fecha"/>
        <TextBox Grid.Row="1" Text="{Binding Fecha}" Grid.Column="1" IsEnabled="False" HorizontalContentAlignment="Left"/>

        <TextBlock Grid.Row="2" Text="Descripción"/>
        <TextBox Grid.Row="2" Text="{Binding Descripcion}" Grid.Column="1" IsEnabled="False"/>

        <TextBlock Grid.Row="3" Text="Descripción adicional"/>
        <TextBox Grid.Row="3" Text="{Binding DescripcionAdicional}" Grid.Column="1"/>

        <TextBlock Grid.Row="4" Text="Monto"/>
        <TextBox Grid.Row="4" Text="{Binding Monto, StringFormat={}{0:C0}.00}" Grid.Column="1"/>

        <TextBlock Grid.Row="5" Text="Cuota"/>
        <TextBox Grid.Row="5" Text="{Binding Cuota}" IsEnabled="False" Grid.Column="1"/>

        <CheckBox Grid.Row="6" HorizontalContentAlignment="Left" Content="Es mensual" IsChecked="{Binding EsMensual}"/>

        <CheckBox Grid.Row="7" HorizontalContentAlignment="Left" Content="Es ajeno" IsChecked="{Binding EsAjeno}"/>

        <ItemsControl ItemsSource="{Binding Tags}" Grid.Row="8" Grid.ColumnSpan="2" Margin="10, 20, 10, 10">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel  />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ToggleButton Content="{Binding Tag}" IsChecked="{Binding Seleccionada}" Margin="5"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Content="Guardar" 
                Command="{Binding SaveCommand}"
                Grid.Column="1"
                Grid.Row="9"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
