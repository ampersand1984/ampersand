<UserControl x:Class="ampersand_pb.Views.MovimientoABMView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" >
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="10"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="10"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
            </Style>
        </Grid.Resources>

        <TextBlock Grid.Row="0" Text="Id movimiento"/>
        <TextBox Grid.Row="0" Text="{Binding IdMovimiento}" Grid.Column="1" IsEnabled="False"/>

        <TextBlock Grid.Row="1" Text="Tipo"/>
        <TextBox Grid.Row="1" Text="{Binding TipoDescripcion}" Grid.Column="1" IsEnabled="False" HorizontalContentAlignment="Left"/>

        <TextBlock Grid.Row="2" Text="Fecha"/>
        <TextBox Grid.Row="2" Text="{Binding Fecha}" Grid.Column="1" IsEnabled="False" HorizontalContentAlignment="Left"/>

        <TextBlock Grid.Row="3" Text="Descripción"/>
        <TextBox Grid.Row="3" Text="{Binding Descripcion}" Grid.Column="1" IsEnabled="False"/>

        <TextBlock Grid.Row="4" Text="Descripción adicional"/>
        <TextBox Grid.Row="4" Text="{Binding DescripcionAdicional}" Grid.Column="1"/>

        <TextBlock Grid.Row="5" Text="Monto"/>
        <TextBox Grid.Row="5" Text="{Binding Monto}" Grid.Column="1"/>

        <TextBlock Grid.Row="6" Text="Cuota"/>
        <TextBox Grid.Row="6" Text="{Binding Cuota}" IsEnabled="False" Grid.Column="1"/>

        <CheckBox Grid.Row="7" HorizontalContentAlignment="Left" Content="Es mensual" IsChecked="{Binding EsMensual}"/>

        <CheckBox Grid.Row="8" HorizontalContentAlignment="Left" Content="Es ajeno" IsChecked="{Binding EsAjeno}"/>

        <ItemsControl ItemsSource="{Binding Tags}" Grid.Row="9" Grid.ColumnSpan="2" Margin="10, 20, 10, 10">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel  />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ToggleButton Content="{Binding Tag}" IsChecked="{Binding Seleccionada}" Margin="5"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Content="Guardar" 
                Command="{Binding SaveCommand}"
                Grid.Column="1"
                Grid.Row="10"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
