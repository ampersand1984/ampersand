<UserControl x:Class="ampersand_pb.Views.MovimientosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:common="clr-namespace:ampersand_pb.Common"
             xmlns:views="clr-namespace:ampersand_pb.Views">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.Resources>
            <common:BooleanToVisibilityConverter x:Key="boolToVis"/>
            <common:NullToVisibilityConverter x:Key="nullToVis"/>
        </Grid.Resources>

        <Controls:TransitioningContentControl x:Name="transitioning" Transition="LeftReplace"
                                              Content="{Binding EditVM}"
                                              Grid.Column="1"
                                              Grid.ColumnSpan="2"
                                              Panel.ZIndex="2"
                                              HorizontalAlignment="Stretch"
                                              HorizontalContentAlignment="Stretch"
                                              VerticalContentAlignment="Stretch"
                                              VerticalAlignment="Stretch"/>

        <DataGrid ItemsSource="{Binding Movimientos}"
                  SelectedIndex="{Binding SelectedIndex}"
                  Style="{StaticResource AzureDataGrid}"
                  HorizontalContentAlignment="Stretch"
                  Grid.Column="0"
                  Grid.ColumnSpan="2"
                  Margin="10"
                  IsReadOnly="True"
                  AutoGenerateColumns="False" 
                  Visibility="{Binding EditVM, Converter={StaticResource nullToVis}, ConverterParameter=not}">
            
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick"
                              Command="{Binding EditarMovimientoCommand}"/>
            </DataGrid.InputBindings>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="IsSelected" 
                            Value="{Binding IsSelected, Mode=TwoWay}" />
                </Style>
            </DataGrid.RowStyle>
            
            <DataGrid.Resources>
                
                <Style x:Key="leftTopAlignment"
                       TargetType="{x:Type DataGridCell}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type DataGridCell}">
                                <Grid Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Left"
                                                      VerticalAlignment="Top"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style x:Key="rightTopAlignment"
                       TargetType="{x:Type DataGridCell}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type DataGridCell}">
                                <Grid Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Right"
                                                      VerticalAlignment="Top"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <!--<Style x:Key="leftHeaderAlignment" TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="OverridesDefaultStyle" Value="False"/>
                </Style>
                <Style x:Key="rightHeaderAlignment" TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                    <Setter Property="HorizontalAlignment" Value="Right"/>
                    <Setter Property="OverridesDefaultStyle" Value="False"/>
                </Style>-->
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" 
                                    Binding="{Binding Fecha, StringFormat='{}{0:dd/MM/yyyy}    '}" 
                                    Width="auto" 
                                    CellStyle="{StaticResource leftTopAlignment}"/>
                
                <DataGridTemplateColumn Header="Descripcion"
                                        Width="5*"
                                        CellStyle="{StaticResource leftTopAlignment}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Descripcion}"
                                           Grid.Row="0"/>
                                <TextBlock Text="{Binding DescripcionAdicional}"
                                           TextWrapping="Wrap"
                                           FontSize="9"
                                           Grid.Row="1"/>
                                <ItemsControl ItemsSource="{Binding Tags}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Bottom"
                                              Grid.Row="2">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}"
                                                       Background="LightGreen"
                                                       Margin="1"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridCheckBoxColumn ElementStyle="{DynamicResource MetroDataGridCheckBox}"
                                        EditingElementStyle="{DynamicResource MetroDataGridCheckBox}"
                                        Header="Mensual"
                                        Binding="{Binding EsMensual}"/>

                <DataGridCheckBoxColumn ElementStyle="{DynamicResource MetroDataGridCheckBox}"
                                        EditingElementStyle="{DynamicResource MetroDataGridCheckBox}"
                                        Header="Ajeno"
                                        Binding="{Binding EsAjeno}"/>

                <DataGridTextColumn Header="Cuota" 
                                    Binding="{Binding Cuota}"
                                    Width="auto"
                                    CellStyle="{StaticResource leftTopAlignment}"/>
                
                <DataGridTextColumn Header="Monto" 
                                    Binding="{Binding Monto, StringFormat={}{0:C0}.00}" 
                                    FontSize="12" 
                                    FontWeight="Bold"
                                    Width="1*"
                                    CellStyle="{StaticResource rightTopAlignment}"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Column="2"
              Margin="10"
              Visibility="{Binding EditVM, Converter={StaticResource nullToVis}, ConverterParameter=not}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Margin="0,0,0,10">
                <TextBlock Text="Total resúmen"
                           HorizontalAlignment="Right" />
                <TextBlock Text="{Binding TotalResumen, StringFormat={}{0:C0}.00, Mode=OneWay}"
                           FontWeight="Bold"
                           HorizontalAlignment="Right" />
            </StackPanel>

            <ItemsControl Grid.Row="1"
                          ItemsSource="{Binding Agrupaciones}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="{Binding Descripcion}"
                                       Background="LightGreen" 
                                       HorizontalAlignment="Right"/>
                            <TextBlock Text="{Binding Monto, StringFormat={}{0:C0}.00}"
                                       HorizontalAlignment="Right"
                                       FontWeight="Bold"
                                       Grid.Row="1"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Grid.Row="2" 
                        Margin="0,0,0,10"
                        Visibility="{Binding AgrupacionesSeleccion, Converter={StaticResource nullToVis}}">
                <TextBlock Text="Total selección"
                           HorizontalAlignment="Right" />
                <TextBlock Text="{Binding TotalSeleccion, StringFormat={}{0:C0}.00, Mode=OneWay}"
                           FontWeight="Bold"
                           HorizontalAlignment="Right" />
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="0,0,0,10">
                <Button Content="Importar info del mes anterior"
                    Command="{Binding ImportarInfoDeResumenAnteriorCommand}"
                    Visibility="{Binding EsElUtimoMes, Converter={StaticResource boolToVis}}"
                    Grid.Row="2"
                    VerticalAlignment="Bottom"
                    Margin="10"/>
                <Button Content="Proyectar"
                    Command="{Binding ProyectarCommand}"
                    Visibility="{Binding EsElUtimoMes, Converter={StaticResource boolToVis}}"
                    Grid.Row="2"
                    VerticalAlignment="Bottom"
                    Margin="10"/>
                <Button Content="Guardar cambios"
                    Command="{Binding SaveCommand}"
                    Grid.Row="2"
                    VerticalAlignment="Bottom"
                    Margin="10"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>